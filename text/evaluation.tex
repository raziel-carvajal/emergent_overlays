%!TEX root = main.tex

\section{Evaluation}
\label{sec:evaluation}
Details about the scenarios, where each broadcasting algorithm is deployed, were given at Section~\ref{sec:scenarios}; besides, we lack a way to compare the performance of these algorithms.
%
Our experimental results are given by the following metrics:

%
\textbf{Saved Rebroadcasts (SRE)} --
number of broadcast messages that nodes do not retransmit based on the worse case scenario, \textit{pure flooding}.
%
To compute the SRE of one algorithm $A$ over $N$ nodes, we count the number of retransmissions $R$ made by $A$ to disseminate one message. Then, we simply say that $A$ saved $N - R$ messages.
%
This metric is useful to measure the efficiency in terms of both network and energy usage. 

\textbf{Duplicated messages} --
number of times one broadcast message is received after its first reception.
%
In a perfect situation nodes receive a messages at most once, typically it is not the case while broadcasting.

\textbf{Reachability} --
percentage of nodes that successfully receive one broadcast message at least once.

\textbf{Broadcast time} --
time to disseminate one message with reachability of 100\%.

% 
%It is only measured when all the participants receive the message because this is the only way of comparing protocols with different coverage. 
\textbf{Energy consumption} --
energy consumed by nodes during every broadcast session.
%
\begin{figure*}[ht]
	\begin{center}
	\subfigure[Static Scenarios]{
		\label{fig:a-saved}
		\includegraphics[scale=0.29]{fig/results-static-summary.pdf}
	}
	~
	\subfigure[Static Scenarios]{
		\label{fig:a-duplicated}
		\includegraphics[scale=0.33, trim = 0 10mm 0 0]{fig/results-static.pdf}
	}
	~
	\subfigure[Mobility Scenarios]{
		\label{fig:b-saved}
		\includegraphics[scale=.29]{fig/results-mobility-summary.pdf}
	}
	~
	\subfigure[Mobility Scenarios]{
		\label{fig:b-duplicated}
		\includegraphics[scale=0.33]{fig/results-mobility.pdf}
	}
	\caption{Saved Rebroadcasts over different de}
	\label{fig:saved-rebroadcasts}
	\end{center}
\end{figure*}

%
We analyze every broadcasting metric with/without mobility over different number of neighbors (range of node densities) as described at Section~\ref{sec:scenarios}.
%
\subsection{Network overload}

SRE and the number of duplicated messages determine to which extent the communication medium is saturated.
%Redundant
%These metrics are useful to assess if a broadcasting algorithm have to send too many messages in order to reach a good coverage.
%Redundant
%This is important because all these messages can quickly saturate the medium and affect the transmission of other messages unrelated to the broadcasting operation.
%
Figure~\ref{fig:a-saved} depicts the SRE in a static scenario where MPRT outperforms all other protocols.
%
The difference between MPRT and all other algorithms decreases as the density increases.
%
Interestingly, \textit{ABBA} rapidly reduce the number of retransmissions even better than a node-degree based technique when the density is above 10.
%
While a node-degree technique overloads the communication medium with its periodic exchange of control messages, ABBA avoids retransmissions because the bigger the communication range of nodes its is covered
%
Finally, \textit{ProbFlood} has a poor performance for all densities.  




The behavior changes when we add mobility to the settings.
%
In those cases, the best results are displayed by algorithms that do not require the construction of an underlaying topology.
%
In particular, \textit{ABBA} is best for all densities.
%
We can even observe how its behavior is similar with and without mobility.
%
On the contrary, \textit{MPR} and \textit{node-degree} are much worse when nodes are moving around.
%
In studying these results we notice that the CDS-based approaches cannot maintain a completely up to date underlaying topology due to the mobility.
%
As a consequence, they do not properly limit the number of nodes that must retransmit a broadcast message.
% 


The number of duplicated messages received for each nodes is shown in Figure~\ref{fig:duplicated-messages}.
%
As this value varies for each node, we are using \textit{boxplots} to present  a distribution for each setting.
%
It is important to highlight that the number of duplicated messages is never equal to the density of the network because there are many collisions causing the lost of messages.
%
For instance, with density 30 and using \textit{simple flooding} we would expect a distribution where most nodes received around 30 duplicated messages.
%
However, as shown in Figure~\ref{fig:a-duplicated}, most nodes barely receive 10 duplicated messages.
%
This simply happens due to the high number of collisions produced when \textit{simple flooding} is chosen.
%
In general, there are three reasons for obtaining a low number of duplicated messages: i) few messages are sent (high number of saved rebroadcasts); ii) there are many collisions; and iii) the coverage is ``low'', meaning that some nodes never receive some messages.


%
Figures~\ref{fig:a-duplicated} and~\ref{fig:b-duplicated} show that \textit{MPR} is again best for all densities with and without mobility.
%
Meanwhile, \textit{node-degree} behaves similarly to \textit{ProbFlood} for low densities; but it outperforms \textit{ProbFlood} for density 20 and beyond.
%
\textit{ProbFlood}'s behavior is specially poor in this metric, it is as bad as \textit{simple flooding} when the density is high.  





\subsection{Energy Consumption}

The following section analyses the energy consumption of the studied approaches.
%
The energy consumption of a node depends on several factors: the \textit{number of message sent}, the \textit{number of messages received}, and the \textit{network saturation in the vicinity} of the node.
%
The \textit{number of message sent and received} are important because, every time the node sends or receives a message, the \textit{transmitter} or \textit{receiver} components of the radio change to a more power hungry mode.
%
Hence, the broadcast messages retransmitted as well as the control messages have a direct influence on energy consumption.
%
The \textit{network saturation in the vicinity} of the node is also important due to the way the MAC layer is implemented.
%
In CSMA, a node must receive an \textit{ACK} notification when a message is received, otherwise the message is resent.
%
The goal of this strategy is to increase the reliability of the network, but it has a strong negative effect in terms of energy consumption when the network is saturated.
%
Since in this case both messages and ACK notifications are often lost, the MAC layer keeps resending message and consuming energy in the process.
%

Figures~\ref{fig:a-energy} and~\ref{fig:b-energy} show the values of energy consumption for the static and mobility settings respectively.
%
Since each node sends and receives different number of messages, we use \textit{boxplots} to present the full distribution of power consumption.
%
Due to space limitations, we do not present the plots for all densities.
%
Instead, we are only showing plots for those densities were there is a significant change in the patterns of energy consumption.
% 

\begin{figure*}[htb]
	\centering
	\subfigure[Static Scenarios]{\label{fig:a-energy}\includegraphics[page=1, width=0.90\textwidth]{fig/results-static.pdf}}
	~
	\subfigure[Mobility Scenarios]{\label{fig:b-energy}\includegraphics[page=1, width=0.90\textwidth]{fig/results-mobility.pdf}}
	\caption{Energy consumption for different densities and mobility scenarios. Each box show the distribution of energy consumption for all the nodes in a scenario.}
	\label{fig:energy-consumption}
\end{figure*}


The first thing to note is that, as expected, \textit{simple flooding} consumes more energy in all scenarios.
%
Also interesting is that both CDS-based approaches, namely \textit{MPR} and \textit{node-degree}, always outperform other approaches when there is no mobility.
%
This is consistent with the results depicted in Figures~\ref{fig:a-saved} and~\ref{fig:a-duplicated}, which show that nodes send and receive fewer messages in these approaches.
%
In the case of \textit{ABBA} and \textit{ProbFlood}, our experiments do not show a single value of density where these algorithms behave better than \textit{MPR}.
%
This is interesting because it indicates that properly using control messages to build an underlying topology is worth in terms of power consumption when the interval among two broadcast messages is close to the interval among control messages.
%

As depicted in Figure~\ref{fig:b-energy}, the behavior of these protocols change in the mobility scenarios.
%
In such scenarios, \textit{ABBA} outperforms \textit{MPR} when the density is low or medium (5, 10, 15).
%
Only when the density is greater than 20, MPR becomes competitive once again.
%
In correlating these results with Figures~\ref{fig:b-saved} and ~\ref{fig:b-duplicated} we notice that the nodes send fewer messages with \textit{ABBA} than with \textit{MPR}.
%
We also notice that the number of duplicated messages received is closer than in the static scenarios.
%
%Although This changes when densities above 30 because the control 
%
Since \textit{MPR} cannot maintain a completely up to date underlaying topology due to the mobility, it fails in reducing the number of relays nodes and the power consumption is high.

These results are of interest because they show two parameters of a scenario that may impact the decision of what algorithm to use for broadcasting.
%
The first parameter is the mobility of nodes and the second is the ratio between the broadcast interval and control message interval.

\subsection{Coverage}

Figure~\ref{fig:coverage} presents the coverage of the five algorithms studied in the scenarios with and without mobility.
%
All these algorithms tend to be reliables in static settings and densities above 10.
%
Some approaches fail to deliver all broadcast messages in settings with very low density because many sections of the network are only connected by a single link that can be point of failure. 
%
In particular, \textit{ABBA} and \textit{MPR} are more prone to this kind of occurrences.
%

The results in the mobility scenarios are less stables.
%
As expected, some protocols such as \textit{MPR} and \textit{node-degree} behave worse because the underlying topology they build to drive the communication must be constantly updated due to the movement of nodes.
%
This problem can be observed for all the densities, but it is more clear in denser topologies.
%
We can also see that \textit{ABBA} tends to fail with different values of density, although far less than the CDS-based approaches.
%
This is understandably because \textit{ABBA} is an area-based approach where each node uses the location of its neighbors to drive the broadcast process.
%
Since the location of each node changes continually, it is expected that this approach won't be 100\% reliably. 

\begin{figure*}[htb]
	\centering
	\subfigure[Static Scenarios]{\label{fig:a}\includegraphics[page=2, width=0.45\textwidth]{fig/results-static-summary.pdf}}
	~
	\subfigure[Mobility Scenarios]{\label{fig:b}\includegraphics[page=2, width=0.45\textwidth]{fig/results-mobility-summary.pdf}}
	\caption{Broadcast coverage for different scenarios: densities, mobility and broadcasting approaches.}
	\label{fig:coverage}
\end{figure*}


Finally, we observe that \textit{simple flooding} is the most reliably algorithm in our experiments.
%
\begin{figure*}[htb]
	\centering
	\subfigure[Static Scenarios]{\label{fig:a-coll}\includegraphics[page=1, width=0.45\textwidth]{fig/collisionsNoMob.pdf}}
	~
	\subfigure[Mobility Scenarios]{\label{fig:b-coll}\includegraphics[page=1, width=0.45\textwidth]{fig/collisionsMob.pdf}}
	\caption{Collisions of broadcasting messages.}
	\label{fig:collisions}
\end{figure*}
This is consistent with previous studies~\cite{YiGerla2003, Garbinato2010}. 

\begin{figure*}[htb]
	\centering
	\subfigure[Static Scenarios]{\label{fig:a-sessT}\includegraphics[page=1, width=0.45\textwidth]{fig/SessionTimeNoMob.pdf}}
	~
	\subfigure[Mobility Scenarios]{\label{fig:b-sessT}\includegraphics[page=1, width=0.45\textwidth]{fig/sessionTimeMob.pdf}}
	\caption{Broadcasting session time.}
	\label{fig:broSessionTime}
\end{figure*}

\begin{figure*}[htb]
	\centering
	\subfigure[Static Scenarios]{\label{fig:a-relays}\includegraphics[page=1, width=0.45\textwidth]{fig/relaysNoMob.pdf}}
	~
	\subfigure[Mobility Scenarios]{\label{fig:b-relays}\includegraphics[page=1, width=0.45\textwidth]{fig/relaysMob.pdf}}
	\caption{Percentage of nodes that performs retransmissions.}
	\label{fig:broSessionTime}
\end{figure*}

