[General]
repeat = 1
# status
**.hasStatus = true
# this lovely and unknown line is the one enabling broadcast in IPv4. We need
# this feature in order to simulate sending simultaneously to many nodes in
# wireless networks
**.networkLayer.ip.forceBroadcast = true
# initial energy
**.energyStorage.nominalCapacity = 100.0J
**.energyStorage.nodeShutdownCapacity = 0J
**.energyStorage.initialCapacity = this.nominalCapacity
# just one application running
*.host*.numUdpApps = 1
# mobility model
*.host*.mobilityType = "StoredMobility"
#
sim-time-limit = SIMULATION_TIME
# application set up
[Config CONFIGURATION_NAME]

network=built_topologies.TOPOLOGY_NAME

**.wlan[*].radio.receiver.ignoreInterference = true
**.wlan[*].radio.transmitter.maxCommunicationRange = NODES_TRANSMISSION_RANGE

**.udpApp[0].startTime = 0s
**.udpApp[0].stopTime = SIMULATION_TIME

**.udpApp[0].sendInterval = BROADCAST_MSG_INTERVAL
**.udpApp[0].ctrlMsgInterval = CTRL_MSG_INTERVAL

# source node
  *.SOURCE_NODE_ID.udpApp[0].isSource = true
# message length
  **.udpApp[0].messageLength = 200B
# communication area set up
  **.udpApp[0].centerDensAx = CENTER_POS_X
  **.udpApp[0].centerDensAy = CENTER_POS_Y
  **.udpApp[0].denseAreaWid = DENSE_REGION_WIDTH
# adaptation set up
  **.udpApp[0].withAdaptation = WITH_ADAPTATION
  **.udpApp[0].adaptationPolicy = ADAPTATION_POLICY
# mobility model
  **.mobility.isMoving = WITH_MOBILITY
  **.mobility.filename = "../../experiments/networks/built_topologies/TOPOLOGY_NAME.mobility"
# concat all parameters of each algorithm
