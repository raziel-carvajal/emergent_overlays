%!TEX root = main.tex

\begin{algorithm}
	\footnotesize
	\DontPrintSemicolon
	
		
		\Fn(\tcp*[f]{on propose check adaptation trigger}){trigger(localTimer)}{
			\eIf{Density >= 15}
			{
				\If{\AlgoPid $\neq$ flooding}
				{
					\FuncSwitch (flooding)\;
				}
				
			}{
				\If{\AlgoPid $\neq$ MPR}
				{
					\FuncSwitch (MPR)\;
				}
			}
		}


\caption{\labalg{LocaPolicy}Local Policy}
\end{algorithm}