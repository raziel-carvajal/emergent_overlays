%!TEX root = main.tex
%=========================================================

\section{Evaluation}
\label{sec:evaluation}

\er{%
proposed introduction outline
\begin{itemize}
	\item MANETs are important due to advances in the IoT and the fact that not all systems will benefit from a fixed infrastructure (too costly, too seldom useful, etc.) \cite{Bellavista2013} -- TODO find more recent references justifying the use of infrastructure-less systems
	\item broadcast fundamental operation in MANETs, including for routing purposes, aggregation etc. \cite{Williams2002,basagni2004mobile} - multiple protocols have been proposed for broadcast in MANETs
	\item broadcast in MANETs happens by the retransmission of a broadcast message by nodes until it reaches all nodes in the system. The objectives is to reach full coverage whilst spending as little per-node and overall energy as possible
	\item the nature of the medium (wireless communication) makes this task complicated, as there can be collisions leading to lost messages, and the intrinsically local decision of whether or not to retransmit a broadcast message is complicated due to the complete absence of synchronization between nodes.
	\item There has been a wealth of work in efficient broadcast algorithms for manets.	
	\item According to the survey by Ruiz and Bouvry~\cite{SurveyRuiz}, protocols can be classified in two main categories: protocols that use pre-established overlays, and those that do not use an established topology. This is similar to the typical unstructured vs structured overlays classification in P2P systems. Give a few details about the way unstructured approaches are based on controlled flooding (and therefore only use information available at the time of the propagation to decide on forwarding decisions) while approaches based on overlays can use information collected a priori and structure established also a priori.
	\item previous studies~\cite{Aschenbruck2009,DensityMobDrivenEval,Williams2002} have shown that the performance and costs of each broadcast approach was intimately influenced by environmental aspects of the MANET and the distribution of hte nodes in space. This has an impact on performance but also on reliability. In particular, density and mobility are key factors. The intuition is the denser the network is, the better it is to use an overlay to avoid collisions and speed up transmission (in less dense networks this is not as helpful and may lead to partitions if not up-to-date); and, conversely, the more mobility there is, the less useful an overlay is since overlays constructed in such context quickly become deprecated, posing risks of having partitions or bad coverage.
	\item choosing the appropriate protocol prior to the deployment of a MANET is difficult for a homogeneous network where the density and mobility patterns are not known a priori
	\item MANETs are often expected to have heterogeneous distribution of nodes over space \cite{Camp2002,bai2004mobility,Ramdhany2008} %(find citations, maybe from~\cite{mobility_density_model}) 
	and the density and mobility in different parts of the system will differ (find some convincing real life use cases to back this up, like the festival scenario)
	\item this makes the problem even more complex as the best algorithm for a part of the network may not be the best for another part.
	\item In this work, we are motivated by the notion that no one size fits all, and we are therefore interested in making broadcasting more adaptive and consequently more efficient and less costly for heterogeneous MANETs.
	\item More specifically we propose that overlay networks and associated broadcast protocols be used only in the parts of the MANETs where they are truly beneficial, whilst the rest of the system is able to use a more flexible protocol that incurs higher costs but resists better to dynamic conditions.
	\item our approach relies on a combination of interoperability and adaptation: (1) we allow different parts of the network to use different broadcast algorithms and ensure that coverage guarantees are at least as good as what the protocols taken in isolation would yield; (2) we propose dynamic adaptation mechanisms that allow nodes to dynamically select appropriate protocols based on the observation of the nodes deployment context. 
	We study the different adaptation metrics available and devise a number (say how many) of adaptation policies allowing nodes to emerge broadcast overlays in a decentralized and autonomous manner based on environmental and deployment conditions.
	\item We evaluate our approach using (include here highlights from the evaluation, including the use of traces etc.)
	\item Our contributions are (recap contributions, e.g.: )
	\begin{itemize}
		\item MAC layer for efficient retransmission
		\item interoperability protocol
		\item adaptation metrics
		\item adaptation policies
	\end{itemize}
	\item Our findings are (recap findings)
	\item paper outline
\end{itemize}
}